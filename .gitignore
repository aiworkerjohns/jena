# Maven and build files
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.flattened-pom.xml
.m2/

# Eclipse
.classpath
.project
.settings
.recommenders
.metadata
maven-eclipse.xml
.externalToolBuilders/
.mvn/

# Idea IntelliJ
*.ipr
*.iws
*.iml
.idea/

# Visual Studio Code
.vscode/

# Caches
.scala_dependencies

# Other
.DS_Store
hs_err_*

# Binaries
*.jar

# Backups
*~
.#*
\#*

# Fuseki file area
run/

# Demo runtime data
demo/DB/
demo/Lucene/
demo/test/DB/
demo/test/Lucene/
demo/test/Taxonomy/

# The geochem-external demo was removed when the demos were consolidated, but its
# multi-GB build output may still be sitting in a working tree. Kept ignored so a
# `git add -A demo/` cannot sweep it back in.
demo/geochem-external/

# Playwright screenshots and report — regenerated by `task test`. These were
# committed previously, which churned a fresh binary blob on every UI change
# (234 versions of 44 PNGs, ~50 MB). Publish them as CI artifacts instead.
demo/testing/results/

# Benchmark JSON results
jena-benchmarks/jena-benchmarks-jmh/*.json

# Docker image vulnerability scan reports (task scan)
build-files/docker/scan-reports/

# Demo: generated classpath for the optional embeddings module, and downloaded models
demo/test/.embeddings-cp
demo/test/models/
